# ─── NeoReLiS Primary Backend Environment ────────────────────────────
# Copy this file to .env and fill in the values

# Database
# PostgreSQL connection string used by Prisma
# Format: postgresql://<user>:<password>@<host>:<port>/<database>?schema=public
DATABASE_URL=postgresql://postgres:password@localhost:5432/neorelis?schema=public

# JWT Authentication
# A strong, random secret key for signing JWT tokens (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Token expiry duration (e.g., 7d, 24h, 60m)
JWT_EXPIRY=7d

# App
# Port to run the backend server on
PORT=9078
# Environment mode: development | production | test
NODE_ENV=development

# Frontend URL (used for CORS configuration)
FRONTEND_URL=http://localhost:3000